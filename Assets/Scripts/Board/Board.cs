using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Board : MonoBehaviour
{
    /*
    public BoardController controller { get; private set; }
    public BoardModel model { get; private set; }
    public BoardView view { get; private set; }

    //[SerializeField] private BoardModel model;
    //[SerializeField] private BoardView view;

    private void Start()
    {
        this.model = new BoardModel();
        var m1 = model.Model;
        this.view = FindObjectOfType<BoardView>();
        this.controller = new BoardController(this.model, this.view);
    }
    */
}
